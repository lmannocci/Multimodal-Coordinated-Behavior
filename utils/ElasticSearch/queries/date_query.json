{
	"query": {
      "bool": {
        "must": {
          "range": {
            "created_at": {
              "gte": "{{startDate}}",
              "lte": "{{endDate}}"
            }
          }
        }
      }
    },
    "sort": [
        {"created_at": "asc"}
    ]
}